#include <stdio.h>
int main(){
    int n;
    printf("Введите размер массива (N): ");
    scanf("%d", &n);
    if (n > 2){
        int a, b, i, s, k;
        int arr[n];
        printf("Введите числа А и В: ");
        scanf("%d %d", &a, &b);
        arr[0] = a;
        arr[1] = b;
        s = a + b;
        for (i = 2; i < n; i++){
            arr[i] = s;
            s += arr[i];
        }
        for (i = 0; i < n; i++)
        printf("%d ", arr[i]);
        return 0;
    }
    else printf("error");
    return 0;
}
