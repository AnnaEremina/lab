#include <stdio.h>
int main(){
   int a, b, i, j;
   printf("Введите два значения длин отрезков А и В: ");
   scanf("%d %d", &a, &b);
   if(a<b){
      printf("Неверные значения!");
      return 0;
   }
   else{
      while (a >= b){
         a = a - b;
      }
      printf("%d", a);
      return 0;
   }
}
